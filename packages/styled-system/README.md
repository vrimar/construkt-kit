# @b3/styled-system

Shared Panda CSS generated runtime for B3 frontend apps.

This package owns the generated `css`, `jsx`, `patterns`, `recipes`, `tokens`,
and `types` entrypoints used by `@b3/ui` and by consuming apps that run Panda.
The generated runtime is emitted into `dist/` during package builds.
