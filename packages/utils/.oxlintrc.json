{
  "extends": ["./node_modules/@b3/config/oxlint/index.json"],
  "rules": {
    // axios is an optional peer dep in this package — allow it here
    "no-restricted-imports": [
      "error",
      {
        "patterns": ["node-fetch"],
        "message": "Use @b3/api instead of direct HTTP libraries"
      }
    ]
  }
}
