import { defineConfig } from "oxfmt";

export default defineConfig({
  semi: true,
  singleQuote: false,
  trailingComma: "all",
  singleAttributePerLine: true,
  printWidth: 100,
  tabWidth: 2,
  sortImports: true,
});
