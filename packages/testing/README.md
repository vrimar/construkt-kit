# @b3/testing

Test infrastructure for B3 apps (MSW request handlers for Storybook). **Dev dependency only.**

This package is currently a stub — no public exports yet. Infrastructure for shared MSW handlers is scaffolded under `src/handlers/` with a `@b3/testing/handlers` sub-path export ready for use.
