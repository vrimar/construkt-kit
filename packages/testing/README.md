# @construkt-kit/testing

Test infrastructure for Construkt Kit apps (MSW request handlers for Storybook). **Dev dependency only.**

This package is currently a stub — no public exports yet. Infrastructure for shared MSW handlers is scaffolded under `src/handlers/` with a `@construkt-kit/testing/handlers` sub-path export ready for use.
