/** Arrow + tip slots shared by the floating surfaces (popover, hover card). */
export const floatingArrow = {
  arrow: {
    "--arrow-size": "sizes.3",
    "--arrow-background": "colors.neutral.surface.bg",
  },
  arrowTip: {
    borderTopWidth: "0.5px",
    borderInlineStartWidth: "0.5px",
  },
} as const;
