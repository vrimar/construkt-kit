{
  "name": "construkt-kit",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vrimar/construkt-kit.git"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint:fix",
    "format": "turbo run format",
    "typecheck": "turbo run typecheck",
    "storybook": "turbo run storybook --filter @construkt-kit/storybook",
    "build-storybook": "turbo run build --filter @construkt-kit/storybook",
    "changeset": "changeset",
    "version-packages": "changeset version && pnpm install --lockfile-only",
    "release": "pnpm build && pnpm turbo run check:exports && changeset publish",
    "update-deps": "pnpm update -r && pnpm update"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.18.4",
    "@changesets/cli": "^2.31.0",
    "@construkt-kit/config": "workspace:*",
    "@typescript/native-preview": "7.0.0-dev.20260701.1",
    "oxfmt": "^0.57.0",
    "oxlint": "^1.72.0",
    "oxlint-tsgolint": "^0.24.0",
    "publint": "^0.3.21",
    "turbo": "^2.10.2"
  },
  "engines": {
    "node": ">=22.18",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@11.7.0"
}
