# Scope the @b3 namespace to the Azure Artifacts feed.
# Replace <organization>, <project>, and <feed> with your actual values
# (visible on the Azure Artifacts → Connect to feed → npm page).
@b3:registry=https://pkgs.dev.azure.com/<organization>/<project>/_packaging/<feed>/npm/registry/
always-auth=true
