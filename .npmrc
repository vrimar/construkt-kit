# Intentionally empty.
# pnpm 11 ignores env-var auth tokens placed in a committed project .npmrc,
# so the npmjs publish token lives in user-level/global pnpm config instead
# (see ~/.npmrc or pnpm's global config; configure via `pnpm config set`).
